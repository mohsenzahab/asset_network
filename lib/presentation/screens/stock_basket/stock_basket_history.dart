import 'package:flutter/material.dart';

class StockBasketHistoryWidget extends StatelessWidget {
  const StockBasketHistoryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(children: []),
    );
  }
}
